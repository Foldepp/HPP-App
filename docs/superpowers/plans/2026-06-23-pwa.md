# PWA „HPP Training" Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Die statische App zu einer installierbaren PWA machen — Home-Bildschirm-Icon, Standalone-Start, Free-Tier offline lauffähig.

**Architecture:** Ein Web-App-Manifest + ein Service Worker, der die App-Shell cache-first ausliefert und `/api/*` nie abfängt. Icons werden aus einem Quell-SVG (Gürtel-Streifen) lokal mit macOS-Bordmitteln (`qlmanage` + `sips`) zu PNGs gerendert und committet. Reine Erweiterung der Vanilla-App, kein Build.

**Tech Stack:** Vanilla JS, Web App Manifest, Service Worker (Cache API), `node:test`, macOS `qlmanage`/`sips` (nur zur einmaligen Icon-Erzeugung).

**Spec:** `docs/superpowers/specs/2026-06-23-pwa-design.md`.

## Global Constraints

- **Kein Build** — generierte Icons werden als PNG committet; nichts wird zur Laufzeit erzeugt.
- **`/api/*` wird vom Service Worker NIE abgefangen/gecacht** (Login/Entitlement müssen live sein).
- **Cache-Versionierung:** `CACHE_NAME` mit Version; alte Caches beim `activate` löschen. Code-Update = Version hochzählen.
- **Farben:** `background_color`/`theme_color` = `#f7f6f3`. Stufenfarben: Gelb `#f2c200`, Grün `#3aa655`, Blau `#2b6cb0`, Braun `#8b5e3c`, Schwarz `#26303a`.
- **Namen:** `name` = „HPP-Prüfungstraining", `short_name` = „HPP Training".
- Icon-Generierung läuft auf **macOS** (qlmanage/sips); die PNGs werden committet, danach ist die App plattformunabhängig auslieferbar.

---

## Task 1: Web-App-Manifest

**Files:**
- Create: `app/manifest.webmanifest`
- Test: `app/manifest.test.js`

**Interfaces:**
- Produces: `manifest.webmanifest` mit Feldern `name`, `short_name`, `start_url`, `display`,
  `background_color`, `theme_color`, `lang`, `icons[]` (verweist auf `icon-192.png`, `icon-512.png`).

- [ ] **Step 1: Failing test schreiben** — `app/manifest.test.js`

```js
const test = require("node:test");
const assert = require("node:assert");
const fs = require("fs");
const path = require("path");

test("manifest.webmanifest: valides JSON mit Pflichtfeldern", () => {
  const raw = fs.readFileSync(path.join(__dirname, "manifest.webmanifest"), "utf8");
  const m = JSON.parse(raw);
  assert.strictEqual(m.name, "HPP-Prüfungstraining");
  assert.strictEqual(m.short_name, "HPP Training");
  assert.strictEqual(m.start_url, "/");
  assert.strictEqual(m.display, "standalone");
  assert.strictEqual(m.background_color, "#f7f6f3");
  assert.strictEqual(m.theme_color, "#f7f6f3");
  assert.ok(Array.isArray(m.icons) && m.icons.length >= 2);
  const sizes = m.icons.map((i) => i.sizes);
  assert.ok(sizes.indexOf("192x192") >= 0 && sizes.indexOf("512x512") >= 0);
  assert.ok(m.icons.every((i) => /maskable/.test(i.purpose)));
});
```

- [ ] **Step 2: Test laufen lassen, Fehlschlag bestätigen**

Run: `node --test app/manifest.test.js`
Expected: FAIL — `ENOENT` (Datei existiert noch nicht).

- [ ] **Step 3: `app/manifest.webmanifest` anlegen**

```json
{
  "name": "HPP-Prüfungstraining",
  "short_name": "HPP Training",
  "lang": "de",
  "start_url": "/",
  "display": "standalone",
  "background_color": "#f7f6f3",
  "theme_color": "#f7f6f3",
  "icons": [
    { "src": "icon-192.png", "sizes": "192x192", "type": "image/png", "purpose": "any maskable" },
    { "src": "icon-512.png", "sizes": "512x512", "type": "image/png", "purpose": "any maskable" }
  ]
}
```

- [ ] **Step 4: Test laufen lassen, Erfolg bestätigen**

Run: `node --test app/manifest.test.js`
Expected: PASS.
Run: `npm test`
Expected: alle Suites grün.

- [ ] **Step 5: Commit**

```bash
git add app/manifest.webmanifest app/manifest.test.js
git commit -m "feat(pwa): Web-App-Manifest (HPP Training, standalone) + Test"
```

---

## Task 2: Icon — Quell-SVG + generierte PNGs

**Files:**
- Create: `app/icon.svg`
- Create (generiert, committet): `app/icon-512.png`, `app/icon-192.png`, `app/apple-touch-icon.png`

**Interfaces:**
- Produces: `icon-192.png`, `icon-512.png` (vom Manifest referenziert), `apple-touch-icon.png` (von index.html).

- [ ] **Step 1: Quell-SVG anlegen** — `app/icon.svg`

(Gürtel-Streifen, zentriert im maskable-Sicherheitsbereich ~ zentrale 80 %.)

```svg
<svg xmlns="http://www.w3.org/2000/svg" width="512" height="512" viewBox="0 0 512 512">
  <rect width="512" height="512" fill="#faf9f6"/>
  <rect x="96" y="114" width="320" height="44" rx="22" fill="#f2c200"/>
  <rect x="96" y="174" width="320" height="44" rx="22" fill="#3aa655"/>
  <rect x="96" y="234" width="320" height="44" rx="22" fill="#2b6cb0"/>
  <rect x="96" y="294" width="320" height="44" rx="22" fill="#8b5e3c"/>
  <rect x="96" y="354" width="320" height="44" rx="22" fill="#26303a"/>
</svg>
```

- [ ] **Step 2: PNGs erzeugen (macOS qlmanage + sips)**

Run:
```bash
qlmanage -t -s 512 -o app app/icon.svg >/dev/null 2>&1
mv app/icon.svg.png app/icon-512.png
sips -z 192 192 app/icon-512.png --out app/icon-192.png >/dev/null
sips -z 180 180 app/icon-512.png --out app/apple-touch-icon.png >/dev/null
```

- [ ] **Step 3: Dimensionen verifizieren**

Run:
```bash
for f in app/icon-512.png app/icon-192.png app/apple-touch-icon.png; do echo "$f:"; sips -g pixelWidth -g pixelHeight "$f" | grep pixel; done
```
Expected: `icon-512.png` 512×512, `icon-192.png` 192×192, `apple-touch-icon.png` 180×180. Schlägt `qlmanage` fehl (kein PNG erzeugt), STOP und melden — dann alternativ `@resvg/resvg-js` als devDependency nutzen.

- [ ] **Step 4: Commit**

```bash
git add app/icon.svg app/icon-512.png app/icon-192.png app/apple-touch-icon.png
git commit -m "feat(pwa): App-Icon Gürtel-Streifen (SVG-Quelle + PNGs 512/192/180)"
```

---

## Task 3: Service Worker

**Files:**
- Create: `app/sw.js`

**Interfaces:**
- Produces: Service Worker, der die App-Shell cache-first bedient und `/api/*` durchlässt.
- Consumes: die Datei-/Icon-Namen aus Tasks 1–2.

- [ ] **Step 1: `app/sw.js` anlegen**

```js
var CACHE_NAME = "hpp-v1";
var SHELL = [
  "/", "/index.html", "/styles.css",
  "/data.js", "/logic.js", "/srs.js", "/entitlement.js", "/app.js",
  "/manifest.webmanifest", "/icon-192.png", "/icon-512.png", "/apple-touch-icon.png"
];

self.addEventListener("install", function (e) {
  e.waitUntil(
    caches.open(CACHE_NAME)
      .then(function (c) { return c.addAll(SHELL); })
      .then(function () { return self.skipWaiting(); })
  );
});

self.addEventListener("activate", function (e) {
  e.waitUntil(
    caches.keys().then(function (keys) {
      return Promise.all(keys.filter(function (k) { return k !== CACHE_NAME; })
        .map(function (k) { return caches.delete(k); }));
    }).then(function () { return self.clients.claim(); })
  );
});

self.addEventListener("fetch", function (e) {
  var req = e.request;
  if (req.method !== "GET") return;
  var url = new URL(req.url);
  if (url.origin !== self.location.origin) return;   // fremde Origins: normal
  if (url.pathname.indexOf("/api/") === 0) return;     // API: immer Netz, nie Cache
  e.respondWith(
    caches.match(req).then(function (hit) {
      if (hit) return hit;
      return fetch(req).then(function (res) {
        if (res && res.ok) {
          var copy = res.clone();
          caches.open(CACHE_NAME).then(function (c) { c.put(req, copy); });
        }
        return res;
      }).catch(function () {
        if (req.mode === "navigate") return caches.match("/");
        return Response.error();
      });
    })
  );
});
```

- [ ] **Step 2: Syntax-Check**

Run: `node --check app/sw.js`
Expected: keine Ausgabe (Syntax OK).

- [ ] **Step 3: Commit**

```bash
git add app/sw.js
git commit -m "feat(pwa): Service Worker — App-Shell cache-first, /api network-only, Versions-Cleanup"
```

---

## Task 4: `index.html` — Manifest, Meta, Apple-Icon, SW-Registrierung

**Files:**
- Modify: `app/index.html`

**Interfaces:**
- Consumes: `manifest.webmanifest`, `apple-touch-icon.png`, `sw.js`.

- [ ] **Step 1: Head-Tags ergänzen**

In `app/index.html` direkt nach der Zeile `<link rel="stylesheet" href="styles.css">` einfügen:

```html
  <link rel="manifest" href="manifest.webmanifest">
  <meta name="theme-color" content="#f7f6f3">
  <link rel="apple-touch-icon" href="apple-touch-icon.png">
  <meta name="apple-mobile-web-app-capable" content="yes">
  <meta name="apple-mobile-web-app-title" content="HPP Training">
```

- [ ] **Step 2: Service-Worker-Registrierung am Body-Ende ergänzen**

In `app/index.html` direkt vor `</body>` (nach dem `<script src="app.js"></script>`) einfügen:

```html
  <script>
    if ("serviceWorker" in navigator) {
      window.addEventListener("load", function () {
        navigator.serviceWorker.register("/sw.js").catch(function () {});
      });
    }
  </script>
```

- [ ] **Step 3: Prüfen**

Run: `grep -c "manifest.webmanifest\|apple-touch-icon\|serviceWorker" app/index.html`
Expected: `3` (alle drei Bezüge vorhanden).
Run: `npm test`
Expected: weiterhin alle Tests grün.

- [ ] **Step 4: Commit**

```bash
git add app/index.html
git commit -m "feat(pwa): index.html — Manifest/Theme/Apple-Icon-Meta + SW-Registrierung"
```

---

## Task 5: Verifikation im Browser + Deploy (Koordinator)

**Files:** keine (Prüfung + Deploy)

- [ ] **Step 1: Unit-Tests grün**

Run: `npm test`
Expected: alle Suites grün (inkl. Manifest-Test).

- [ ] **Step 2: Lokal prüfen**

Server `hpp-app` (Static, Port 8123) über die preview_*-Tools starten, `http://localhost:8123/` laden.
- Konsole fehlerfrei; Service Worker registriert (preview_eval `navigator.serviceWorker.controller` bzw. `.getRegistrations()`).
- Manifest erreichbar: `/manifest.webmanifest` lädt als JSON; `/icon-192.png`, `/icon-512.png`, `/apple-touch-icon.png` liefern 200.
- **Offline-Test:** nach erstem Laden in den DevTools/`preview_eval` einen Reload simulieren, der nur den Cache nutzt — Free-Tier (Gelb/Grün) muss rendern. (Mind. prüfen, dass alle Shell-Dateien im Cache liegen: `caches.open('hpp-v1').then(c=>c.keys()).then(k=>k.map(r=>r.url))`.)
- Screenshot des Icons / der App als Beleg.

- [ ] **Step 3: Deploy**

Run: `/Users/cwick/.npm-global/bin/vercel --prod --yes`
Expected: Deployment `READY` auf `https://hpp-app-one.vercel.app`.

- [ ] **Step 4: Prod-Check**

```bash
curl -s -o /dev/null -w "manifest: %{http_code}\n" https://hpp-app-one.vercel.app/manifest.webmanifest
curl -s -o /dev/null -w "sw.js:    %{http_code}\n" https://hpp-app-one.vercel.app/sw.js
curl -s -o /dev/null -w "icon-192: %{http_code}\n" https://hpp-app-one.vercel.app/icon-192.png
curl -s -o /dev/null -w "apple:    %{http_code}\n" https://hpp-app-one.vercel.app/apple-touch-icon.png
```
Expected: alle `200`. Danach auf dem iPhone: Safari → Teilen → „Zum Home-Bildschirm" → Gürtel-Icon + „HPP Training" erscheinen, Start im Standalone-Look.

- [ ] **Step 5: Abschluss**

STATUS.md/Memory aktualisieren (Koordinator): PWA live, Dark Mode bleibt als nächstes optionales Polish-Item.

---

## Self-Review (durchgeführt)

**1. Spec-Abdeckung** (`2026-06-23-pwa-design.md`):
- §3.1 Manifest mit Pflichtfeldern → Task 1 (+ Test).
- §3.2 Icons (SVG-Quelle + PNG 192/512 maskable + apple-touch 180) → Task 2.
- §3.3 Service Worker (Precache Shell, cache-first, `/api` network-only, Versions-Cleanup) → Task 3.
- §3.4 index.html (manifest, theme-color, apple-touch-icon, apple-meta, SW-Registrierung) → Task 4.
- §4 Erfolgskriterien (installierbar, iOS-Icon, Free-Tier offline, /api nicht gecacht, Update via Version) → Task 5 + die SW-Logik in Task 3.
- §5 Verifikation (Manifest-JSON-Test + Browser) → Task 1 (Unit) + Task 5 (Browser/Deploy).
- §6 NICHT in Scope (Push/Background-Sync/App-Store/Offline-Bezahlinhalt) → nirgends gebaut.

**2. Placeholder-Scan:** Keine TBD/TODO. Der resvg-Fallback in Task 2 Step 3 ist eine bedingte Eskalation (nur falls qlmanage fehlschlägt — ist vorab erfolgreich getestet), kein offener Platzhalter.

**3. Typ-Konsistenz:** `CACHE_NAME` „hpp-v1" konsistent (Task 3). Datei-/Icon-Namen (`icon-192.png`, `icon-512.png`, `apple-touch-icon.png`, `manifest.webmanifest`) identisch über Manifest (Task 1), Icon-Gen (Task 2), SW-Shell (Task 3) und index.html (Task 4). `start_url:"/"` deckt sich mit der SW-Navigations-Fallback `caches.match("/")`.
